services:
  mongo:
    image: mongo:6
    container_name: local-mongo
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: credit-card-bill-transactions
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
